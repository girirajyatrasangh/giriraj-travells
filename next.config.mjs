/** @type {import('next').NextConfig} */
const nextConfig = {
  // TypeScript errors now BLOCK the build — catches bugs before they reach users
  // images.unoptimized removed — Vercel edge optimization is now active
}

export default nextConfig
